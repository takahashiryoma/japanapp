<h1>id: <%= @user.id %> のユーザーの編集ページ</h1>

<%= form_with(model: @user) do |f| %>
  <%= f.label :name, '名前' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', users_path %>